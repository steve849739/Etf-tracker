# 环境变量模板 —— 复制为 .env 后按需填写（.env 已被 .gitignore 忽略，不会入库）
# 本项目无密钥类配置（无 Token/密码），以下变量均可省略，代码内有默认值。
# 仅当你想覆盖默认路径时才需要设置。

ETF_LIST_PATH=./etf清单.xlsx
ETF_TRACK_PATH=./etf跟踪.xlsx
ETF_DATA_PATH=./data/etf_data.json
